"""
Scheme implementations — concrete adapters for :class:`AuthScheme`.

Each sub-package (oauth2, api_key, …) exports a single adapter class that
implements the scheme port.  Internal mechanics are private.
"""
