"""
Protocol implementations — concrete adapters for :class:`AuthProtocol`.

Each sub-package (oauth2, …) exports a single adapter class that
implements the protocol port.  Internal mechanics are private.
"""
